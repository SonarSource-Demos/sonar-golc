name: Stub Build and Manual Approval Release

on:
  push:
    branches:
      - FoxDev  # Only run on changes to the FoxDev branch
  workflow_dispatch: # Allows manual triggers

jobs:
  # Stub Build Job
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Stub Build Process
        run: echo "Building the Go application... (stubbed)"

      - name: Run Tests (Stub)
        run: echo "Running tests... (stubbed)"

  # Manual Approval Job
  manual_approval:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Wait for manual approval
        uses: chrnorm/deploy-approval-action@v1.2.0
        with:
          approvers: 'SonarFox'  # Replace with the GitHub username(s) who can approve
          token: ${{ secrets.GITHUB_TOKEN }}

  # Stub Release Job
  release_stub:
    runs-on: ubuntu-latest
    needs: manual_approval
    steps:
      - name: Stub Release Process
        run: echo "Release process started... (stubbed)"
